import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    template: "%s | Mithun Das — AI Business Automation",
    default: "Mithun Das — AI Business Automation Engineer",
  },
  description:
    "Operational AI systems for businesses that have outgrown manual workflows. Automation architecture connecting CRM, WhatsApp, email, AI assistants into reliable workflows.",
  metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL || "https://mithundas.cloud"),
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
